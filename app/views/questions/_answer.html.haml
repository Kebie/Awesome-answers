- if @answer.errors.any?
  %h3.field_with_errors ERRRORSSSSS!!!!!!
  .alert.alert-danger
    %ul
      - @answer.errors.full_messages.each do |m|
        %li= m

/ = form_for [@question, @answer] do |f|
= form_for @answer, url: question_answers_path(@question) do |f|
  = f.text_area :body
  = f.submit "Submit an answer"